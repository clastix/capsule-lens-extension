import React from "react";

export const Metrics = () => {
  const style = {
    marginBottom: 'calc(2 * var(--unit))',
    height: 'calc(30 * var(--unit))',
    fontSize: '2em',
    backgroundColor: 'var(--drawerSubtitleBackground)'
  }
  
  return (
    <div className='flex justify-center align-center' style={style}>
      Metrics Placeholder
    </div>
  );
};
